spring:
  cloud:
    config:
      enabled: true
      fail-fast: true
      username: dumars
      password: p@ssw0rd
      discovery:
        enabled: true
        service-id: config
#    stream:
#      kafka:
#        binder:
#          brokers: localhost:9092
#          zk-nodes: localhost:2181
  zipkin:
    base-url: http://localhost:9411
