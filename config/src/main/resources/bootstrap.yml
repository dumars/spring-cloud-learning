spring:
  application:
    name: config

  cloud:
    config:
      server:
        git:
          uri: https://github.com/dumars/spring-cloud-repo/
          search-paths: /{application}
          username:
          password:
          clone-on-start: true
      profile: dev
      label: develop